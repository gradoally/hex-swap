#include "stdlib.fc";

global cell nft_swap_code;
global cell nft_swap_contracts;
global cell nft_swap;

() recv_internal(int my_balance, int msg_value, cell in_msg_full, slice in_msg_body) impure {

    if (op = op::mint_swap_contract) {
      ;; 
    }

    

}
